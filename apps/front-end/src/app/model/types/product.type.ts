export type APIProduct = {
  id: string;
  name: string;
  price: string;
  categoryId: string;
};

export type Product = {
  id: string;
  name: string;
  price: string;
  categoryId: string;
  categoryName: string;
};
